============
Installation
============

At the command line::

    $ easy_install unach-photo-server

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv unach-photo-server
    $ pip install unach-photo-server
